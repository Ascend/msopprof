minimum_pre_commit_version: 4.0.0
exclude: ^LICENSES/|\.(html|csv|svg)$

default_stages: [pre-commit]
ci:
  autofix_prs: false
  autoupdate_schedule: monthly

repos:
  # pre-commit 基础检查
  - repo: https://gitcode.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: ["--allow-multiple-documents"]
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: detect-private-key
      - id: check-json
        exclude: ^.devcontainer/devcontainer.json$

  # codespell
  - repo: https://gitcode.com/gh_mirrors/co/codespell
    rev: v2.4.1
    hooks:
      - id: codespell
        args: [
            "-L",
            "CANN,cann,NNAL,nnal,ASCEND,ascend,EnQue,CopyIn,ArchType,AND,ND,tbe,copyin,alog,thirdparty,nd,datas,Datas,DEPENDEES",
            "--skip",
            "*.py,*.cpp,*.hpp,*.c,*.h,*.toml",
          ]

  # typos
  - repo: https://gitcode.com/gh_mirrors/ty/typos
    rev: v1.32.0
    hooks:
      - id: typos
        args: ["--force-exclude", "--config", "pre-commit/typos.toml"]

  #--------------- C++ 核心检查 ---------------------------------------------
  - repo: https://gitcode.com/pre-commit-clang/mirrors-clang-format
    rev: v18.1.8
    hooks:
      - id: clang-format
        files: \.(c|h|cpp|hpp|cc|hh|cxx|hxx)$
        entry: git-clang-format
        args: [--binary, clang-format, -f]
        exclude: \.(build|test|third-party)

  # -------------------------- Gitleaks 本地离线二进制扫描 --------------------------
  - repo: local
    hooks:
      - id: gitleaks-offline-scan
        name: Gitleaks Secret Scan(Local Binary)
        entry: ./gitleaks
        language: system
        pass_filenames: true
        args:
          - protect
          - --verbose
          - --redact
          - --config=pre-commit/.gitleaks.toml
        stages: [pre-commit]
  # --------------- C++ 静态检查（本地 clang-tidy）------------------
  # 仅扫描 csrc/ 下的源文件（.cpp/.c/.cc/.cxx），头文件通过 --header-filter 间接检查
  # --header-filter 限定仅报告 csrc/ 下头文件的告警，不报告 msopscommon/、thirdparty/ 等
  - repo: local
    hooks:
      - id: clang-tidy
        name: clang-tidy (本地venv)
        entry: clang-tidy
        language: system
        pass_filenames: true
        files: ^csrc/.+\.(c|cpp|cc|cxx)$
        args:
          - "--config-file=.clang-tidy"
          - "-p"
          - "."
          - "--header-filter=^csrc/"
